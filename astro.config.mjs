// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://RenanGirao.github.io',
  base: '/Observatorio-Nacional',
  devToolbar: { enabled: false },
});
